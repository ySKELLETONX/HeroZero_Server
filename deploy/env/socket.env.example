# /etc/herozero/socket.env  — lido por herozero-socket.service
HZ_SOCKET_HOST=127.0.0.1
HZ_SOCKET_PORT=8090
# Segredo do endpoint /push. DEVE ser igual a HZ_SOCKET_TOKEN do laravel.env.
HZ_SOCKET_TOKEN=troque-por-um-segredo-forte
